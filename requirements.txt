# Context Compressor Skill 依赖

# 核心依赖
python>=3.8

# 标准库依赖（通常已内置）
# json, re, logging, datetime, pathlib, csv

# 可选依赖（用于高级功能）
# nltk>=3.8  # 自然语言处理（用于语义分析）
# numpy>=1.24  # 数值计算（用于统计分析）
# pandas>=2.0  # 数据分析（用于日志分析）
# matplotlib>=3.7  # 数据可视化（用于统计图表）

# 开发依赖
pytest>=7.4  # 测试框架
black>=23.0  # 代码格式化
flake8>=6.0  # 代码检查
mypy>=1.5  # 类型检查

# 注意：此Skill设计为轻量级，大部分功能使用Python标准库
# 如需高级功能（如语义分析），请取消注释相应的依赖